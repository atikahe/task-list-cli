// ESM syntax is supported.
/**
 * 
 * @param {*} args
 * [] show existing task list
 * [] complete a task
 * [] get & send update
 * [] could work offline 
 */
export function cli(args){
  console.log(args)
}
